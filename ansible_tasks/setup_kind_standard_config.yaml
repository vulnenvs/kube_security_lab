---
  - name: Start a kind cluster
    command: kind create cluster --image=kindest/node:{{kubernetes_version}} --name {{ cluster_name }}
  - name: Add the host
    add_host: name={{ container_name }}
